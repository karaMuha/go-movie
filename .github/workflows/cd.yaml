name: CD

on:
  push:
    branches: [main]

jobs:
  ratings_service:
    name: Build and push ratings service image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}
      
      - name: Build and push image
        run: |
          docker build . --tag ghcr.io/go-movie/ratings-service:latest -f ./rating/Dockerfile
          docker push ghcr.io/go-movie/ratings-service:latest
  
  metadata_service:
    name: Build and push metadata service image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}
      
      - name: Build and push image
        run: |
          docker build . --tag ghcr.io/go-movie/metadata-service:latest -f ./metadata/Dockerfile
          docker push ghcr.io/go-movie/metadata-service:latest
  
  movie_service:
    name: Build and push movie service image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}
      
      - name: Build and push image
        run: |
          docker build . --tag ghcr.io/go-movie/movie-service:latest -f ./movie/Dockerfile
          docker push ghcr.io/go-movie/movie-service:latest